import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kronos.chiron',
  appName: 'Chiron',
  webDir: 'dist/chiron-front/browser',
};

export default config;
